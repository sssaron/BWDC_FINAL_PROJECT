import { defineConfig } from 'vite'
import { svelte } from '@sveltejs/vite-plugin-svelte'

export default defineConfig({
  plugins: [svelte()],
  base: '/BWDC_FINAL_PROJECT/', // exact repo name, case-sensitive
})